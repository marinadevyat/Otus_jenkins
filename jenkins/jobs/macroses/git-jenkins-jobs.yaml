- scm:
    name: git-ui-tests
    scm:
    - git:
        url: https://github.com/marinadevyat/Otus_web.git
        credentials-id: jenkins
        branches:
        - FETCH_HEA
        refspec: ${BRANCH}
